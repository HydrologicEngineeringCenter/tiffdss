name: Docker Image CI

on:
  push:
    branches: [ "stable","develop" ]
  pull_request:
    branches: [ "stable","develop" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag tiffdss-test:$(date +%s)
